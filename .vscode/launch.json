{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Go CLI",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "console": "integratedTerminal" // Allows stdin input
    }
  ]
}